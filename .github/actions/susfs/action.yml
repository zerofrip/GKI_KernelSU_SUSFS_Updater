name: "SUSFS Action"
description: "Minimal composite action to verify local action detection"

runs:
  using: "composite"
  steps:
    - name: Execute SUSFS Action
      run: echo "SUSFS action executed successfully"
      shell: bash
